@model List<FinalWebApp.Data.Entities.Table>
<div class="row">
    <table>
        <tr>
            <th>Position</th>
            <th>Name</th>
            <th>Capacity</th>
            <th>Status</th>
            <th>Action</th>
        </tr>
        @if (Model != null)
        {
            var position = 1;
            foreach (var item in Model)
            {
                <tr>
                    <td>@(position++)</td>
                    <td>@item.Name</td>
                    <td>@item.Capacity</td>
                    <td>@item.Status</td>
                    <td>
                        <a href="~/Table/Edit?idTable=@item.Id">Update</a>

                        @using (Html.BeginForm("Delete", "Table", FormMethod.Post))
                        {
                            @Html.Hidden("id", item.Id)
                            <button type="submit" onclick="return confirm('Are you sure you want to delete this item?');">Delete</button>
                        }
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3">Empty data</td>
            </tr>
        }
    </table>
</div>
